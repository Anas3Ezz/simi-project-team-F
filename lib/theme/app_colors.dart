import 'package:flutter/material.dart';

class AppColors {
  // 1. Primary Brand Colors
  static const Color primaryTeal = Color(
    0xFF0097B2,
  ); // Buttons, logo text, active states
  static const Color secondaryTeal = Color(
    0xFF82C4C1,
  ); // Light labels (Denteeth), payment text
  static const Color lightTealBackground = Color(
    0xFFE6F5F6,
  ); // Icon backgrounds, selected chips

  // 3. Functional Colors
  static const Color successGreen = Color(
    0xFF70B9BE,
  ); // Payment success checkmark
  static const Color ratingGold = Color(0xFFFFB300); // Star icons for ratings
  static const Color white = Color(0xFFFFFFFF); // Screen backgrounds
  static const Color shadowColor = Color(
    0x1A000000,
  ); // 10% opacity black for elevation
}
