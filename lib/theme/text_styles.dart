import 'dart:ui';

class AppTextStyle {
  // 2. Text & Grayscale
  static const Color textBlack = Color(0xFF000000); // Main headings, titles
  static const Color textGrayPrimary = Color(
    0xFF4F4F4F,
  ); // Body text, descriptions
  static const Color textGraySecondary = Color(
    0xFF9E9E9E,
  ); // Placeholder text, small details
  static const Color fieldBackground = Color(
    0xFFF5F8FA,
  ); // Input fields, unselected chips
}
